package blockchain

/*
type (
	// Blockchain is an interface for blockchains
	Blockchain interface {
		Name() string
		Algorithm() Algorithm
		Length() uint64
		AddBlock(uint64)
		GetBlock(uint64) *Block
	}
)
*/
